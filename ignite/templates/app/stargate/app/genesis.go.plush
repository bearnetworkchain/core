package app

import (
	"encoding/json"

    "github.com/cosmos/cosmos-sdk/codec"
)

// 區塊鏈的創世狀態在這裡表示為原始 json 的映射
// 由標識符字符串鍵入的消息。
// 標識符用於確定屬於哪個模塊創世信息
// 所以它可以在初始化鏈期間被適當地路由。
// 在這個應用程序中，默認的創世信息是從
// 從每個 BasicModule 填充 json 的 ModuleBasicManager
// 在初始化期間提供給它的對象。
type GenesisState map[string]json.RawMessage

// NewDefaultGenesisState 為應用程序生成默認狀態。
func NewDefaultGenesisState(cdc codec.JSONCodec) GenesisState {
    return ModuleBasics.DefaultGenesis(cdc)
}
